# No EOL conversion anywhere: this repo lives under OneDrive on Windows.
# autocrlf/renormalize churn caused `git add -A` timeouts and phantom
# hundreds-of-files "LF will be replaced by CRLF" storms. Files are stored
# and checked out byte-for-byte.
* -text

*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.mp4 binary
*.webm binary
*.zip binary
*.gif binary
